<h1> <%= @instructor.name %> </h1>

<h3> Average Student Age: <%= @instructor.avg_age %> </h3>

<h3> Students: </h3>

<ul>
    <% @instructor.sort_students.each do |student| %>
        <li>
            <%= link_to student.name, student_path(student) %>
        </li>
    <% end %>
</ul>